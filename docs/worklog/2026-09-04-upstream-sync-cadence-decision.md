# [2026-09-04] upstream 동기 «정기 축» 판정 — ★**격차 기반 `behind ≥ 20`**(시간 기반 기각)

**티켓**: `rustjava-upstream-sync-cadence-decision`
**채택 제안**: `2026-09-04-sync-contract-stale-assets-decision#p0`
**성격**: ★**판정 회차** — 검사기·크론·워크플로 신설 **0** · `.rs` **0줄** · `allow` 9곳 무접촉 · ★**S9 미개시**

---

## 1. ★전제 실측 — 먼저 쟀다

### ⒜ 지금의 격차

```
git rev-list --count origin/main..upstream/main   → ★0
git merge-base origin/main upstream/main          → bd42427 (= upstream HEAD)
```
★**behind = 0**(요구된 값) · upstream HEAD `bd42427`(2026-08-31 · bitflags bump) · 마지막 착지 `61d5bf3f`.

### ⒝ ★과거 비용 — 그리고 그것이 제안의 전제를 «반증»한다

회차별 실측(§5 표 그대로 · 게이트②가 검증한 수):

| 회차 | 커밋 | 새 충돌 | 성격 | 게이트② 사이클 |
|---|---|---|---|---|
| S1 | 5 | 2 | 설계(tracing) | 1 |
| S2 | 1 | **5** | 설계(charset) | 1 |
| S3 | 1 | ★**9** | 설계(오류 분류) | 1 |
| S4 | 8 | **0** | 물량 | ★**3** |
| S5 | 6 | 3 | 물량 | 1 |
| S6 | ★**11** | **0** | 물량 | 1 |
| S7 | 1 | 1 | 물량+ | 1 |
| S8 | ★**12** | 8 | 개명 스윕 | 1 |
| **합** | **45** | **28**(중앙 **2.5**) | | **10**(8회차) |

★★**상관계수 r = −0.169**(커밋 수 ↔ 새 충돌) — **사실상 0이고 부호는 «음»이다.**
★**커밋 많은 4회차(S4·S5·S6·S8 = 37커밋) → 충돌 «11»** ↔ ★**적은 4회차(S1·S2·S3·S7 = 8커밋) → 충돌 «17»**.

★**누적 충돌은 «포화»한다**(§5 첫 표 · 고정 base 에서 각 컷을 한 번에 머지했을 때):
7커밋 **16** → 10커밋 17 → 21커밋 17 → 32커밋 17 → 33커밋 **19**.
⇒ ★**7커밋 뒤로 26커밋을 더 벌려도 «+3»**.

⇒ ★★★**「벌어진 뒤에 받으면 훨씬 비싸다」는 이 저장소 데이터로 «반증»된다.**
충돌을 만든 것은 **양이 아니라 «무엇이 왔는가»** — 우리 포크 지점(tracing span · charset 라우팅 · 오류 분류)을
upstream 이 건드릴 때, 그리고 **개명 스윕 같은 사건**이 올 때다.

★**「8회차가 들었다」를 «격차 33 의 비용»으로 읽지 마라** — 회차 경계는 §5 의 **컷 분할**이 정했고,
추가 비용의 큰 덩어리는 ★**스쿼시로 계보가 접힌 것**이다(별 리니지 `…-squash-defeats-convergence`).
그것은 격차가 아니라 **머지 방식**의 비용이고 `merge_strategy: merge` 로 **이미 닫혔다**.

### ⒞ upstream 속도 — 축을 고르려면 이 수가 필요했다(아무도 안 쟀다)

`upstream/main` 최근 **12개월**: **144 커밋 / 51주** —
주당 **평균 2.81 · 중앙 1.0 · p90 7** · ★**커밋이 «0»인 주가 19주(37%)**.

| 누적 격차 | 도달까지(연속 창 · 중앙) | 최소 | 최대 |
|---|---|---|---|
| behind 5 | 8일 | 0일 | 70일 |
| behind 10 | 21일 | 0일 | 111일 |
| ★**behind 20** | ★**44일** | 9일 | 139일 |
| behind 33 | 83일 | 34일 | 169일 |

## 2. ★판정 — ⒝ **격차 기반 · `behind ≥ 20`**

> ★**동기 회차는 `git rev-list --count origin/main..upstream/main` 이 «20 이상»일 때 연다.**
> ★**시간(격주·월간)으로 열지 않는다.** ★**그 아래에서는 열지 않는다.**

### ⒜ ★시간 기반(⒜)을 기각한 이유 — 둘 다 실측이다

⑴★**upstream 이 37% 의 주에 커밋 «0»** 이고 버스티하다(p90 7/주) ⇒ 시간 축은 ★**behind 0 인데 회차를 여는 «빈 회차»** 를 만든다.
⑵★**충돌이 포화하므로 «작게 자주»가 총비용을 낮추지 않는다** — 회차 오버헤드는 **고정**이다(티켓 **3건**/회차 ·
게이트② 사이클은 **8회차 중 7회가 1**). ⇒ ★**충돌 총량이 그대로인데 회차 수만 늘면 «총비용이 는다».**

### ⒝ ★사건 기반(⒞)을 기각한 이유

「우리 포크 지점 파일을 upstream 이 건드리면」이 그럴듯해 보이지만 ★**데이터가 안 맞는다**:
S3 의 충돌 9건은 ★**upstream 의 «신규» 파일**에서 왔고(우리 파일이 아니다), S8 의 8건은 ★**전 트리 개명**이었다.
⇒ ★**경로 기반 방아쇠는 실제 비용의 대부분을 «놓친다».**

### ⒞ ★정기 축을 두지 않는 것(⒟)을 기각한 이유

★**오늘 «아무것도» 알려 주지 않는다.** behind 는 사람이 생각날 때만 재고, 이 캠페인이 그 상태에서 33까지 갔다.
★그리고 ⒝ 의 포화는 **7~33 구간에서만** 측정됐다 — 33 너머는 ★**미측정**이고 33 은 실제로 아팠던 값이다.
⇒ ★**미측정 구역으로 걸어 들어가지 않는다.**

### ⒟ ★왜 «20» 인가 — 위아래를 둘 다 적는다

- **더 내리지 않는다**: 충돌이 **7커밋에서 포화**하므로 그보다 자주 받아도 충돌 총량이 안 준다(회차 수만 는다).
- **더 올리지 않는다**: 33 은 **미측정 구역의 입구**이고 실제로 아팠다.
- ★**늦게 알수록 «우리 자산 낡음»이 오래 방치된다** — S8 개명 스윕이 `rust.yml` 크레이트 이름과 경로 4곳을
  낡게 만들었고, 그 여파가 **문서 정정 5회차**로 이어졌다(§4 리니지).
- 실측 도달 시간 **중앙 44일** ⇒ ★**연 6~8회**.

## 3. ★고른 축의 «비용»

| 축 | 실측값 |
|---|---|
| 회차당 **새 충돌** | 중앙 **2.5** · 범위 **0~9**(S5~S8 만 보면 3·0·1·8) |
| 회차당 **게이트② 사이클** | **1**(8회차 중 7회) · 최악 **3**(S4) |
| 회차당 **티켓** | **3**(구현 · 게이트② · 게이트③) |
| 연간 회차 | **6~8**(behind 20 도달 중앙 44일) |
| ⇒ **연간 티켓** | **18~24** + 사이클 초과분 |

★**예측이 아니라 «지금까지의 값»이다.** ⒡ 의 재측정 조건이 이 표를 갱신한다.

## 4. ★누가 방아쇠를 당기는가 — **기계가 «재고», 사람(총괄)이 «발권한다»**

```sh
git fetch -q upstream && git rev-list --count origin/main..upstream/main   # ★오늘 = 0
```

- ★**재는 자리(권고) = 이 repo 의 «예약 워크플로»**. 선례가 있다 — `rust-audit.yaml` 이 이미 `schedule: cron` 을 쓴다.
  upstream 을 **read-only fetch** 해 그 수를 찍고, 임계 초과 시 **눈에 보이게** 알린다.
  ★**upstream 발신 0**(fetch 는 «읽기»다 — PR·이슈·코멘트·push 아님).
- ★**대안 = 총괄 레인의 `bin/healthcheck` 축**(그 레인에 이미 체크아웃이 있다).
  ★**그것은 `orchestrator` repo 소관이라 이 저장소가 정할 수 없다** ⇒ ★**별건 판정**으로 넘긴다.
- ★★**발권은 «사람(총괄)»이 한다 — 기계가 자동으로 동기 회차를 열지 않는다.**
  이유: 회차마다 **설계 판단**(우리 포크 지점을 유지할지)이 들어가고 S1·S2·S3 에서 **실제로 필요했다**.
- ★**구현은 «별건»이다** — 이 회차는 검사기·크론·워크플로를 **하나도 만들지 않았다**(티켓 non-goal).

## 5. ★재측정 조건 — 세는 법과 오늘의 값

> ★**새 축으로 «3회차»를 돈 뒤 다시 재라.** 임계 **20** 은 «시작값»이지 상수가 아니다.

재잴 것 셋: ⑴임계 도달 간격(실제 일수) ⑵회차당 새 충돌 ⑶게이트② 사이클 수.
- ★**⑵의 중앙이 «5 이상»으로 오르면 임계를 «내려라»** — 포화 가정이 깨진 것이다.
- ★**⑴이 «14일 이하»로 짧아지면 임계를 «올려라»** — upstream 이 빨라진 것이다(지금 중앙 **44일**).

★**오늘의 값**: behind **0** · upstream HEAD `bd42427`(2026-08-31) · 마지막 착지 `61d5bf3f`.
★★**「오래됐다」는 재개 사유가 «아니다»** — 이 축은 **시간이 아니라 격차**로 열린다.

## 6. 적은 «자리»

`docs/upstream-sync-approach.md` **§5-B** — ★**동기 회차가 반드시 읽는 계약 파일**이고,
§5(단계 분할)와 §4(우리 자산 낡음) 사이라 「어디서 자를까 / 언제 열까」가 **같은 자리에** 모인다.
★**워크로그에만 적지 않았다**(그 규율은 이 리니지가 이미 세웠다).

## 7. 경계

★**S9 미개시**(behind **0** — 애초에 열 것이 없다) · 검사기·크론·워크플로 **신설 0** ·
`.rs` **0줄** · `allow` 9곳 **무접촉** · `rust.yml` **무접촉** · `CLAUDE.md` DoD 블록 **무접촉** ·
머지 **0** · force-push **0** · `main` 직접 push **0** · ★**upstream 발신 0**(`git fetch` 읽기만) ·
시크릿 출력 **0** · 맨 `grep` **0**.
