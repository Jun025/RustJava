# [2026-09-05] 워크로그 «부재»를 기계로 잡을지 판정 — ★**지금은 넣지 않는다**(사전 등록 임계 둘 다 미발화)

**티켓**: `rustjava-worklog-absence-machine-enforcement-decision`
**채택 제안**: `2026-09-04-worklog-mandate-and-local-gate#p0`
**성격**: ★**판정 회차** — `scripts/check-worklog-json.py` **무접촉**(6축 무회귀) · `.rs` **0줄** · 과거 backfill **0**

---

## 1. ★사전 등록된 «재측 시점»에 도달했다 — 그래서 이 판정이 열렸다

`AGENTS.md` §Round Worklog 가 의무화 회차에서 **미리** 적어 둔 것: 「**recount at the 10th round landed after 2026-09-04**」.
★**실측: 2026-09-04 이후 착지 회차 «11»** ⇒ ★**조건 충족.**
⇒ ★★**골대를 옮기지 않았다** — 그때 등록한 두 임계를 **그대로** 재서 판정한다.

| # | 커밋 | 날짜 |
|---|---|---|
| 1~9 | `a71676e` `1983d9f` `a0b5d3c` `3e02f8c` `3fb08a8` `a76b305` `7b2dcc4` `61d5bf3` `ba4bb41` | 2026-09-04 |
| 10~11 | `18fbb6e` `4b91e8f` | 2026-09-05 |

## 2. ★지금 잰 작성률 — 창과 분모를 밝힌다

**창** = `b3a4cf4..origin/main`(규약 착지 → 현 `main` `4b91e8f`) · **분모** = `git rev-list --first-parent` 착지 회차.

| 분모 | 착지 회차 | 미작성 | 작성률 |
|---|---|---|---|
| 규약 착지 이후 **전건** | **15** | ★**1** | **93.3%** |
| ★**규약을 «알 수 있었던» 회차만** | **14** | ★**0** | ★**100%** |

★**제안의 「4회차 중 1건」을 옮겨 적지 않았다** — 창이 그때보다 넓어졌고(4 → 15) 값도 다시 쟀다.

### ⒜ 그 «1건»이 정상 참작인지도 «쟀다»

| 축 | 값 |
|---|---|
| 미작성 커밋 | `11ef5010`(PR **#13** · S2 · 2026-08-27 착지 · **squash** 라 부모 1개) |
| PR #13 `createdAt` | **2026-08-23T23:37:41Z** |
| 규약 착지 `b3a4cf4` | **2026-08-26T02:57:28Z** |
| 판정 | ★**PR 이 규약보다 «2.6일» 먼저 생성됐다** ⇒ 정상 참작 — **인용이 아니라 실측**이다 |

### ⒝ 술어의 «약점»도 먼저 쟀다

`AGENTS.md` 정본 술어는 **「`docs/worklog/` 를 만졌다」** 라서 ★**옛 워크로그에 한 줄 추가**도 통과시킨다
(이 리니지의 게이트③ 원장 동봉이 실제로 그런 커밋을 만든다).
⇒ ★**엄격 술어(«새 `.json` 을 추가»했는가 · `--name-status` 의 `A`)로 다시 쟀다**:

| 술어 | 통과 |
|---|---|
| 느슨(만졌다) | **14 / 15** |
| ★엄격(새 `.json` 추가) | ★**14 / 15** |

⇒ ★**이번 창에서는 «같은 수»라 약점이 물리지 않았다.** ★**그러나 약점은 남아 있고**, 그래서 재개 조건은 **엄격 술어**로 적었다.

## 3. ★열린 카드(⒝ 임계) — `< 5` 면 의무 자체를 재검토

`.json` **15**개 · `proposals` 합 **26** − (`adoptedProposals` + `declinedProposals`) **13** = ★**열린 카드 13**
(의무화 시점 기준선 **5** → **13**).
⇒ ★**임계 미달** — 의무가 카드를 **줄이지 않고 늘렸다**.

## 4. ★★결정문

> ★**「워크로그 «부재»」를 잡는 기계 강제를 «지금은 넣지 않는다».**
> ★**`check-worklog-json.py` 의 「존재하는 `.json` 만 검사한다」 설계와 «소급 금지»는 그대로 둔다.**

**근거는 «사전 등록된 두 임계»가 «둘 다» 발화하지 않았다는 것 하나다**:
⒜ 미작성 **1**(정상 참작분을 빼면 **0**) < 임계 **2** · ⒝ 열린 카드 **13** ≥ 임계 **5**.
★★**그 임계는 «데이터를 보기 전»에 등록됐다** — 지금 그것을 넘어서 기계를 넣는 것은
★**이 저장소가 반복해 규탄한 「골대 옮기기」**다. ⇒ **등록한 규칙을 지킨다.**

### ★약점을 숨기지 않는다 — 이것이 재개 조건의 근거다

- ⒤★**「100%」는 «단일 집행 주체»의 습관일 수 있다.** 리니지는 **13개**로 갈렸으나(2026-08-27~09-05)
  ★**git author 가 하나라 «누가 집행했나»를 가릴 수 없다.** ⇒ 일반화의 근거로 쓰지 마라.
- ⒥★**정본 술어는 느슨하다**(§2⒝) — 이번엔 안 물렸을 뿐이다.
- ⒦★**실패는 여전히 «조용하다»** — 부재는 어떤 검사도 울리지 않는다. ★**오늘 그 대가를 치르지 않았을 뿐이다.**

## 5. ★재개 조건 — 세는 명령 + 오늘의 값

> ★**「규약을 알 수 있었던 회차 중 미작성」이 «1건이라도» 나오면 이 결정을 다시 연다**(= 위 명령 값이 **2 이상**).
> ★**「오래됐다」·「불안하다」는 재개 사유가 «아니다».**

```sh
for c in $(git rev-list --first-parent b3a4cf4..origin/main); do
  git diff --name-status "$c^1" "$c" \
    | /usr/bin/grep -q '^A[[:space:]]*docs/worklog/.*\.json$' || echo "$c no-worklog"
done | /usr/bin/grep -c .
```
★**오늘의 값 = `1`**(그 1건 = 규약 이전 분기 `11ef5010`) — ★**문서에 적은 값을 «실행해» 확인했다**.

## 6. ★재개하면 «무엇을» 만들지 미리 적어 둔다

그때 설계부터 시작하지 않도록, 티켓 계약 2 가 요구한 세 칸을 **지금** 채워 둔다:

| 계약 | 미리 정한 답 |
|---|---|
| **소급 금지 유지** | ★**baseline sha 를 쓰지 마라** — 소급 금지를 깨고 기준선 자체가 낡는다 |
| ⒜-1 **판정 술어** | ★**PR diff 로 «조건부» 판정**: 「`REPORT.md` 후속 추천 구역을 만졌으면 `docs/worklog/` 에 **새 쌍**도 있어야 한다」. ★**의무가 조건부이므로 검사도 조건부여야 한다** ⇒ baseline 불요 · 소급 0 |
| ⒜-2 **정상 참작 경로** | ★**「그 PR 이 규약 착지보다 먼저 생성됐는가」**(`gh pr view --json createdAt`). ★**제목·라벨로 봐주지 마라** — 문구는 바뀌고 라벨은 붙이면 그만이다 |
| 6축 보존 | ★**「부재」는 «별 축»** — 기존 스키마 6축을 약화하지 않는다 |

## 7. 무회귀 · 경계

★**`scripts/check-worklog-json.py` 무접촉** — `python3 scripts/check-worklog-json.py` **rc=0**(14파일 0문제) ·
docstring 의 「It never demands a `.json` for an existing `.md` — the convention is not retroactive.」 **그대로**.
★과거 회차 **backfill 0** · `qts` 의 같은 잠금 **무접촉** · upstream 동기 리니지 **무접촉** ·
`scripts/check-dod-ci-parity.py` **무접촉**(rc=0) · `.rs` **0줄** · 머지 **0**.
★게이트③은 ★**`--squash` 금지 저장소**다(`contracts/upstream-sync-repos.conf` 등재 — 스쿼시는 계보를 접는다) ⇒ `merge_strategy: merge`.
