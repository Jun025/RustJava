# 2026-09-23 — `## 다음` 정본 결정 (rustjava-2026-09-23-stale-next-pointer-and-euc-kr-boundary-adopt-p1)

채택: `2026-09-23-stale-next-pointer-and-euc-kr-boundary#p1`. 결정 = **⒝ 얇은 층**.

## 반증 먼저
- ⒜ 세 재발은 같은 형태(닫힌 항목이 절에 남음)인가 — **예**, 그리고 형태가 하나 더 좁혀진다: ★**지목을 쓴 회차 ≠ 닫은 회차**.
  ③번 실측: `6521c79f`(09-11 10:58)가 「다음 실작업 = ③의 null-guard」를 **썼고**, `6da7d66f`(09-11 17:22 · #36)가 그 일을 **닫으면서 절을 안 건드렸다**.
  ⇒ 사료의 「쓰인 날 이미 낡아 있었다」는 부정확하다 — 쓸 때는 옳았고 **6시간 뒤** 낡았다.
- ⒝ 규정이 있었나 — **있었다**. 「닫히는 즉시 닫아라」가 `4bb796de`(2026-08-27)부터 STATE.md 에 있다. 이후 2회 더 깨졌다.
  안 지켜진 이유 = 닫는 회차의 티켓은 `## 다음` 을 모른다(티켓 본문에 없다). 반면 카드는 발권 티켓이 `adoptedProposals` 기록을 **명시 요구**해 닫는 회차가 알고 닫는다.

## 비교
| 선택지 | 비용 | 잃는 것 |
|---|---|---|
| ⒜ 현행(손 갱신) | 재발 3회/27일 · 최근 1회 `LANE_IDLE` 1088분 · 중복 발권 1 | 0 |
| ⒝ 얇은 층 | 절 1회 재작성 · 본문 3항목(ref 9 · PR 1) | 산문 근거(사료로 이동) · 낡음 자체는 남되 줄당 조회 1회로 보인다 |
| ⒞ 카드 정본 · 절 폐지 | 0 | ★tower 술어 열린 카드 **0**(전 32건 injected)이라 정본이 「할 일 없음」을 말함 · 선행 사슬 1(노드 4) · 카드 밖 항목 1(PR #81) |

⒞가 결정적으로 탈락한 이유: `open = 전체 − adopted − declined − injected − dismissed`(wie 회차 술어) 로 재면 이 레인 **0**(전체 102 · adopted 67 · declined 3 · injected 32 · dismissed 0).
`− injected` 를 빼면 **32**. 그 32 중 이 레인이 칠 수 있는 이름을 15개 조회하니 이름 일치 티켓이 있는 것 **4**(queued 2 · 미완 2), **없는 것 11**
(예: `link-lambdametafactory#p0/#p1` 주입 09-17 21:58 → `directives/processed/` 로 처리됐는데 `tasks/` 에 그 ref 문자열 0).
⇒ 카드만 두면 이 11건은 어디에도 안 보인다. ※이름 기반 조회라 다른 이름 티켓은 못 잡는다 — 「없다」가 아니라 「이름 일치 0」.

## 재측(⑵)
새 ⓪ 의 ref 8개 전건: origin/main `docs/worklog/*.json` 의 adopted/declined 에 **0회** ⇒ 전건 열림. `java.lang.invoke` 디렉터리 부재(0). PR #81 `OPEN`·`CONFLICTING`(워밍 후 재조회).

## 총괄 몫(발권 측 · 이 레인은 무접촉)
- 주입(`injected`)되고 `directives/processed/` 로 간 RustJava 카드 중 이름 일치 티켓이 없는 11건의 처분 확인 — tower 가 이미 숨겼으므로 발권도 기각 기록도 없으면 조용히 사라진다.

## 후속
없음(새 제안 0 · 검사기 0).
